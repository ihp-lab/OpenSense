<UserControl
    x:Class="OpenSense.WPF.Components.Kvazaar.ImageToPictureConverterConfigurationControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:OpenSense.WPF.Components.Kvazaar"
    xmlns:util="clr-namespace:OpenSense.WPF.Components.Utilities;assembly=OpenSense.WPF.Components.Contracts"
    xmlns:interop="clr-namespace:KvazaarInterop;assembly=KvazaarInterop"
    xmlns:comp="clr-namespace:OpenSense.Components.Kvazaar;assembly=OpenSense.Components.Kvazaar.Windows"
    mc:Ignorable="d"
    d:DesignHeight="450"
    d:DesignWidth="800">
    <d:UserControl.DataContext>
        <comp:ImageToPictureConverterConfiguration />
    </d:UserControl.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition
                Height="auto" />
            <RowDefinition
                Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Grid.Column="0">
            Chroma Format
        </TextBlock>
        <ComboBox
            Grid.Row="0"
            Grid.Column="1"
            SelectedItem="{Binding ChromaFormat}"
            ItemsSource="{Binding Source={util:EnumBindingSource {x:Type interop:ChromaFormat}}, Mode=OneTime}" />

        <TextBlock
            Grid.Row="1"
            Grid.Column="0">
            Output Bit Depth
        </TextBlock>
        <TextBox
            Grid.Row="1"
            Grid.Column="1"
            Text="{Binding OutputBitDepth, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            ToolTip="Bit depth of output Picture samples (e.g. 8, 10, 16)." />
    </Grid>
</UserControl>
