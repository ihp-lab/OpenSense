<UserControl
    x:Class="OpenSense.WPF.Components.FFMpeg.FileSourceConfigurationControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:OpenSense.WPF.Components.FFMpeg"
    xmlns:util="clr-namespace:OpenSense.WPF.Components.Utilities;assembly=OpenSense.WPF.Components.Contracts"
    xmlns:interop="clr-namespace:FFMpegInterop;assembly=FFMpegInterop"
    xmlns:comp="clr-namespace:OpenSense.Components.FFMpeg;assembly=OpenSense.Components.FFMpeg.Windows"
    mc:Ignorable="d"
    d:DesignHeight="450"
    d:DesignWidth="800">
    <d:UserControl.DataContext>
        <comp:FileSourceConfiguration />
    </d:UserControl.DataContext>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition
                Height="auto" />
            <RowDefinition
                Height="auto" />
            <RowDefinition
                Height="auto" />
            <RowDefinition
                Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Grid.Column="0">
            Filename
        </TextBlock>
        <Grid
            Grid.Row="0"
            Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition
                    Width="*" />
                <ColumnDefinition
                    Width="auto" />
            </Grid.ColumnDefinitions>

            <TextBox
                Grid.Column="0"
                Text="{Binding Path=Filename, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <Button
                Grid.Column="1"
                Name="ButtonOpenFile"
                Click="ButtonOpenFile_Click">
                Open
            </Button>
        </Grid>

        <TextBlock
            Grid.Row="1"
            Grid.Column="0">
            Target Format
        </TextBlock>
        <ComboBox
            Grid.Row="1"
            Grid.Column="1"
            SelectedItem="{Binding TargetFormat}"
            ItemsSource="{Binding Source={util:EnumBindingSource {x:Type interop:PixelFormat}}, Mode=OneTime}"
            ToolTip="Select the output pixel format. Set to None to skip frame conversion." />

        <TextBlock
            Grid.Row="2"
            Grid.Column="0">
            Target Width
        </TextBlock>
        <TextBox
            Grid.Row="2"
            Grid.Column="1"
            Text="{Binding TargetWidth, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            ToolTip="Set the target width in pixels for the output frames. Set to 0 to calculate width based on the frame's aspect ratio and target height. If both are 0, uses the frame's original width." />

        <TextBlock
            Grid.Row="3"
            Grid.Column="0">
            Target Height
        </TextBlock>
        <TextBox
            Grid.Row="3"
            Grid.Column="1"
            Text="{Binding TargetHeight, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            ToolTip="Set the target height in pixels for the output frames. Set to 0 to calculate height based on the frame's aspect ratio and target width. If both are 0, uses the frame's original height." />

    </Grid>
</UserControl>
