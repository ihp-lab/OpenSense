<UserControl
    x:Class="OpenSense.WPF.Components.FFMpeg.FileWriterConfigurationControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:OpenSense.WPF.Components.FFMpeg"
    xmlns:util="clr-namespace:OpenSense.WPF.Components.Utilities;assembly=OpenSense.WPF.Components.Contracts"
    xmlns:comp="clr-namespace:OpenSense.Components.FFMpeg;assembly=OpenSense.Components.FFMpeg.Windows"
    mc:Ignorable="d"
    d:DesignHeight="450"
    d:DesignWidth="800">
    <d:UserControl.DataContext>
        <comp:FileWriterConfiguration />
    </d:UserControl.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition
                Height="auto" />
            <RowDefinition
                Height="auto" />
            <RowDefinition
                Height="auto" />
            <RowDefinition
                Height="auto" />
            <RowDefinition
                Height="auto" />
            <RowDefinition
                Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Grid.Column="0">
            Filename
        </TextBlock>
        <Grid
            Grid.Row="0"
            Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition
                    Width="*" />
                <ColumnDefinition
                    Width="auto" />
            </Grid.ColumnDefinitions>

            <TextBox
                Grid.Column="0"
                Text="{Binding Path=Filename, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                ToolTip="Path to the output MP4 video file. Click 'Save' button to browse and select location." />

            <Button
                Grid.Column="1"
                Name="ButtonSaveFile"
                Click="ButtonSaveFile_Click">
                Save
            </Button>
        </Grid>


        <TextBlock
            Grid.Row="1"
            Grid.Column="0">
            Append Timestamp to Filename
        </TextBlock>
        <CheckBox
            Grid.Row="1"
            Grid.Column="1"
            IsChecked="{Binding TimestampFilename, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock
            Grid.Row="2"
            Grid.Column="0">
            Target Width
        </TextBlock>
        <TextBox
            Grid.Row="2"
            Grid.Column="1"
            Text="{Binding TargetWidth, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            ToolTip="Target video width in pixels. Set to 0 to calculate width based on first frame's aspect ratio and target height. If both are 0, uses first frame's original width." />

        <TextBlock
            Grid.Row="3"
            Grid.Column="0">
            Target Height
        </TextBlock>
        <TextBox
            Grid.Row="3"
            Grid.Column="1"
            Text="{Binding TargetHeight, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            ToolTip="Target video height in pixels. Set to 0 to calculate height based on first frame's aspect ratio and target width. If both are 0, uses first frame's original height." />

        <TextBlock
            Grid.Row="4"
            Grid.Column="0">
            GOP Size
        </TextBlock>
        <TextBox
            Grid.Row="4"
            Grid.Column="1"
            Text="{Binding GopSize, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            ToolTip="Group of Pictures size. Set to 0 for intra-only encoding (every frame is a keyframe). Higher values reduce file size but may affect seek performance." />

        <TextBlock
            Grid.Row="5"
            Grid.Column="0">
            Max B-Frames (NOT SUPPORTED)
        </TextBlock>
        <TextBox
            Grid.Row="5"
            Grid.Column="1"
            Text="{Binding MaxBFrames, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            ToolTip="Maximum number of B-frames between reference frames. Set to 0 for no B-frames (lower latency). Higher values improve compression but add encoding delay." />
    </Grid>
</UserControl>
