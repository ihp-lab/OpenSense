<UserControl
    x:Class="OpenSense.WPF.Components.HM.Mp4MuxerConfigurationControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:comp="clr-namespace:OpenSense.Components.HM;assembly=OpenSense.Components.HM.Windows"
    mc:Ignorable="d"
    d:DesignHeight="150"
    d:DesignWidth="800">
    <d:UserControl.DataContext>
        <comp:Mp4MuxerConfiguration />
    </d:UserControl.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <!-- 0: Filename -->
            <RowDefinition
                Height="auto" />
            <!-- 1: Timestamp Filename -->
            <RowDefinition
                Height="auto" />
            <!-- 2: Discard Remaining On Stop -->
            <RowDefinition
                Height="auto" />
        </Grid.RowDefinitions>

        <!-- Row 0: Filename -->
        <TextBlock
            Grid.Row="0"
            Grid.Column="0">Filename</TextBlock>
        <Grid
            Grid.Row="0"
            Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition
                    Width="*" />
                <ColumnDefinition
                    Width="auto" />
            </Grid.ColumnDefinitions>
            <TextBox
                Grid.Column="0"
                Text="{Binding Path=Filename, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Button
                Grid.Column="1"
                Name="ButtonSaveFile"
                Click="ButtonSaveFile_Click">Save</Button>
        </Grid>

        <!-- Row 1: Timestamp Filename -->
        <TextBlock
            Grid.Row="1"
            Grid.Column="0">Append Timestamp to Filename</TextBlock>
        <CheckBox
            Grid.Row="1"
            Grid.Column="1"
            IsChecked="{Binding TimestampFilename, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            ToolTip="Append a timestamp suffix to the base filename to preserve the originating timestamp of the first frame." />

        <!-- Row 2: Discard Remaining On Stop -->
        <TextBlock
            Grid.Row="2"
            Grid.Column="0">Discard Remaining On Stop</TextBlock>
        <CheckBox
            Grid.Row="2"
            Grid.Column="1"
            IsChecked="{Binding DiscardRemainingOnStop, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            ToolTip="When enabled, discard remaining queued frames on stop. When disabled, write all remaining frames before closing the file." />
    </Grid>
</UserControl>
